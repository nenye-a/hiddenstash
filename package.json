{
  "name": "hiddenstash",
  "scripts": {
    "dev": "concurrently --kill-others \"yarn --cwd backend dev\" \"yarn --cwd admin start\"",
    "test": "yarn --cwd backend test && yarn --cwd admin test",
    "postinstall": "yarn --cwd backend install && yarn --cwd admin install"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^5.1.0"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all",
    "arrowParens": "always"
  },
  "private": true
}
