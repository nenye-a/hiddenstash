{
  "name": "hiddenstash",
  "scripts": {
    "dev": "concurrently --kill-others \"yarn --cwd backend dev\" \"yarn --cwd admin start\" \"yarn --cwd extension start:dev\"",
    "test": "yarn --cwd backend test && yarn --cwd admin test && yarn --cwd extension test",
    "postinstall": "yarn --cwd backend install && yarn --cwd admin install && yarn --cwd extension install"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^5.1.0"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all",
    "arrowParens": "always"
  },
  "private": true
}
